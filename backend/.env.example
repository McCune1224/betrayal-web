DATABASE_URL=postgres://postgres:postgres@localhost:5432/social_deduction
TEST_DATABASE_URL=postgres://postgres:postgres@localhost:5432/social_deduction_test
PORT=8080
# Example extraction patterns for migration:
# migrate -database $(cat .env | grep DATABASE_URL | cut -d '=' -f2) -path internal/db/migrations up
# migrate -database $(cat .env | grep TEST_DATABASE_URL | cut -d '=' -f2) -path internal/db/migrations up

